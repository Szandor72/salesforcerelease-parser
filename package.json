{
  "name": "salesforcerelease-parser",
  "version": "0.0.1",
  "description": "Expects an array of salesforce release note urls and parses them into a json",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "salesforce"
  ],
  "author": "szandor72",
  "license": "MIT",
  "dependencies": {
    "scrape-it": "^5.2.3"
  }
}
